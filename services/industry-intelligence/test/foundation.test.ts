import { IndustryIntelligenceService } from '../src/IndustryIntelligenceService'; describe('Industry Intelligence Service', () => { let service: IndustryIntelligenceService; beforeAll(async () => { service = new IndustryIntelligenceService(); await service.initialize(); }); test('should initialize successfully', () => { expect(service).toBeDefined(); }); test('should identify JOLT domains', () => { expect(service.isJoltDomain('facebook.com')).toBe(true); expect(service.isJoltDomain('random.com')).toBe(false); }); });
